// src/version.ts
// Bump these together with android/app/build.gradle (versionCode / versionName)
// on every release. The update banner compares APP_VERSION_CODE against the
// latest_version_code stored in the app_config table.
export const APP_VERSION_CODE = 2
export const APP_VERSION_NAME = '1.1'
